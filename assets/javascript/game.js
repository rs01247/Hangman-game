// INITIALIZATION
var wins = 0;
var userGuess = 0;
var choose = "";

// HTML ELEMENT NODES
var winSpan = document.getElementById("wins");
var userSpan = document.getElementById("userGuess");
var chooseSpan = document.getElementById("choose");
var avengers = ["Thanos", "Hulk", "Thor", "Hawkeye", "Loki"]
var selction = avengers[Math.floor(Math.random() * avengers.length)];


document.onkeyup = function(event) {
    var userKey = event.key

    if userKey === {

    }


}